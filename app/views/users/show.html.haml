.error
  %h2 電話番号の確認
  = form_for @user do |f|
    .field
      = f.label :携帯電話の番号
      %br/
      = f.text_field :tel,placeholder:'携帯電話の番号を入力'
    %h3 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
    .actions
      = f.submit "SMSで送信する"
    %h3 ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。



